diff --git a/tests/convolutional.c b/tests/convolutional.c
--- a/tests/convolutional.c
+++ b/tests/convolutional.c
@@ -55,7 +55,7 @@ void assert_test_result(correct_convolutional *conv, conv_testbench **testbench,
 }

 int main() {
-    srand(time(NULL));
+    srand(1);

     conv_testbench *testbench = NULL;

diff --git a/tests/convolutional-shim.c b/tests/convolutional-shim.c
--- a/tests/convolutional-shim.c
+++ b/tests/convolutional-shim.c
@@ -65,7 +65,7 @@ void assert_test_result(correct_convolutional *conv, void *fec,
 }

 int main() {
-    srand(time(NULL));
+    srand(1);

     conv_testbench *testbench = NULL;

diff --git a/tests/reed-solomon.c b/tests/reed-solomon.c
--- a/tests/reed-solomon.c
+++ b/tests/reed-solomon.c
@@ -41,7 +41,7 @@ void run_test(correct_reed_solomon *rs, unsigned int msg_len, unsigned int num_e
 }

 int main() {
-    srand(time(NULL));
+    srand(1);

     size_t block_length = 255;
     size_t min_distance = 32;
diff --git a/tests/reed-solomon-shim-interop.c b/tests/reed-solomon-shim-interop.c
--- a/tests/reed-solomon-shim-interop.c
+++ b/tests/reed-solomon-shim-interop.c
@@ -56,7 +56,7 @@ void run_test(correct_reed_solomon *rs, void *rs_shim,
 }

 int main() {
-    srand(time(NULL));
+    srand(1);

     size_t block_length = 255;
     size_t min_distance = 32;
